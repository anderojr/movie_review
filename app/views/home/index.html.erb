<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>

<%= link_to 'Sign Out', destroy_user_session_path, data: {"turbo-method": :delete} if user_signed_in? %>
<%= link_to 'Sign In', new_user_session_path if !user_signed_in? %>